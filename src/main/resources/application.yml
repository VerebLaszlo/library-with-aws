spring:
  application:
    name: library
  profiles:
    active: prod

cloud:
  aws:
    stack:
      auto: false
    region:
      static: us-east-1

dynamo:
  readCapacity: 1000
  writeCapacity: 1000

staticContent:
  imageUrlPrefix: img/
